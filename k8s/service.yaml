apiVersion: v1
kind: Service
metadata:
  name: getting-started
  labels:
    app: getting-started
spec:
  type: NodePort
  selector:
    app: getting-started
  ports:
  - port: 80
    targetPort: 8000
